<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <meta charset="UTF-8" />
    <title>Análise de Liberação Combo</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;600&display=swap"
      rel="stylesheet"
    />
    <style>
      html,
      body {
        height: 100%;
      }

      h1,
      h3,
      p {
        margin: 0;
      }

      body {
        display: flex;
        flex-direction: column;
        margin: 0 auto;
        font-size: 16px;
        font-family: "Roboto", sans-serif;
        position: relative;
        color: #333;
      }

      header .wrapper {
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 0.75rem;
      }

      main {
        display: flex;
        flex-direction: column;
        flex: 1;
        margin-top: 0.5rem;
      }

      main .wrapper {
        flex: 1;
        padding: 0.9rem 1rem;
      }

      h1 {
        font-size: 1.375rem;
        font-weight: 400;
        line-height: 1;
      }

      h3 {
        font-size: 1.0625rem;
        font-weight: 700;
        line-height: 1.3;
        margin-top: 0.125rem;
        margin-bottom: 0.5rem;
      }

      p {
        font-size: 0.875rem;
        font-weight: 400;
        line-height: 1.3;
        margin: 0.125rem 0;
      }

      strong {
        font-weight: 600;
      }

      footer {
        text-align: center;
        padding: 0.5rem;
        color: #b6b6b6;
      }

      .container {
        display: flex;
        flex-direction: column;
        flex: 1;
        padding: 1.25rem 1.25rem 0 1.25rem;
      }

      .bg-image {
        position: absolute;
        width: calc(100% - 2.2rem);
        bottom: 0;
        object-fit: contain;
      }

      .box-gradient-container {
        background: radial-gradient(at left bottom, #e87d1e, #003c7c);
        padding: 0.0625rem;
      }

      .box-gradient {
        background: #fff;
      }

      .logo-image {
        width: 6.5rem;
        margin-bottom: -0.375rem;
      }

      .box {
        border: 0.0625rem solid #333;
        padding: 0.5rem 0.8rem;
        margin: 0.625rem 0;
      }

      .row {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        flex-wrap: wrap;
        justify-content: space-between;
      }

      .button {
        font-size: 0.8125rem;
        border-radius: 0.125rem;
        padding: 0.6rem 0;
        font-weight: 400;
        text-align: center;
        color: #fff;
        width: 12rem;
        margin-top: 0.32rem;
        margin-bottom: 0.4375rem;
      }

      .line {
        height: 0.0313rem;
        background-color: #333;
        margin: 0.3125rem 0;
      }

      .button.error {
        background: #d90d26;
      }

      .button.success {
        background: #30b70e;
      }
    </style>
  </head>

  <body>
    <div class="container">
      <header class="box-gradient-container">
        <div class="box-gradient wrapper">
          {{> logo }}
          <h1>Análise de Liberação Combo</h1>
          <div class="logo-image"></div>
        </div>
      </header>
      <main class="box-gradient-container">
        <div class="box-gradient wrapper">
          <div class="box">
            <h3>Solicitante</h3>
            <p>
              <strong>Razão Social: </strong>
              {{ company.name }}
            </p>
            <p>
              <strong>CNPJ: </strong>
              {{ company.cnpj }}
            </p>
            <p>
              <strong>E-mail do Usuário: </strong>
              {{ company.email }}
            </p>
          </div>
          <div class="box">
            <h3>Vínculo</h3>
            <p>{{ analysis_config_string }}</p>
          </div>
          <div class="box row">
            <div>
              <h3>Dados da Pessoa</h3>
              <p>
                <strong>Nome: </strong>
                {{ person_analysis.name }}
              </p>
              <p>
                <strong>CPF: </strong>
                {{ person_analysis.document }}
              </p>
              <p>
                <strong>RG: </strong>
                {{ person_analysis.rg }}
              </p>
            </div>
            <div>
              {{#person_analysis.isApproved}}
              <div class="button success">
                <strong>Perfil: </strong>
                {{ person_analysis.analysis_result_string }}
              </div>
              {{/person_analysis.isApproved}} {{^person_analysis.isApproved}}
              <div class="button error">
                <strong>Perfil: </strong>
                {{ person_analysis.analysis_result_string }}
              </div>
              {{/person_analysis.isApproved}}
              <p>
                <small
                  ><strong>Data da Análise: </strong>
                  {{ person_analysis.created_at_formatted }}</small
                >
              </p>
              <p>
                <small
                  ><strong>Vigência da Análise: </strong>
                  {{ person_analysis.finished_at_formatted }}</small
                >
              </p>
            </div>
          </div>
          <div class="box">
            {{#vehicle_analysis}}
            {{#index}}
            <div class="line"></div>
            {{/index}}
            <div class="row">
              <div>
                {{^index}}
                <h3>Dados da Frota</h3>
                {{/index}}
                <p>
                  <strong>Placa: </strong>
                  {{ item.plate }}
                </p>
                <p>
                  <strong>CPF/CNPJ: </strong>
                  {{ item.owner_document }}
                </p>
                <p>
                  <strong>Proprietário/Arrendatário: </strong>
                  {{ item.owner_name }}
                </p>
              </div>
              <div>
                {{#item.isApproved}}
                <div class="button success">
                  <strong>Perfil: </strong> {{ item.analysis_result_string }}
                </div>
                {{/item.isApproved}} {{^item.isApproved}}
                <div class="button error">
                  <strong>Perfil: </strong> {{ item.analysis_result_string }}
                </div>
                {{/item.isApproved}}
                <p>
                  <small
                    ><strong>Data da Análise: </strong>
                    {{ item.created_at_formatted }}</small
                  >
                </p>
                <p>
                  <small
                    ><strong>Vigência da Análise: </strong>
                    {{ item.finished_at_formatted }}</small
                  >
                </p>
              </div>
            </div>
            {{/vehicle_analysis}}
          </div>
        </div>
      </main>
    </div>
    <footer>
      <p>CÓDIGO DE VERIFICAÇÃO INTERNA: {{ verification_code }}</p>
    </footer>
    {{> background }}
  </body>
</html>
